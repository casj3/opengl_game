
enum State
{
	initializeAvatar,
	initializeScene1,
	scene1,
	editor
};

enum ProgramTypes
{
	default_program,
	skinning,
	normalMapSkinning,

	num_programs
};

enum Skeletons
{
	avatar,

	num_skeletons
};

enum Surfaces
{
	test,

	num_surfaces
};
